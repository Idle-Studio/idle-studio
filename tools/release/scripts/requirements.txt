# Pinned with compatible-release specifiers (~=): patch updates are allowed,
# minor/major are not. A breaking `cryptography` release must never land in the
# middle of a release run — it signs the App Store Connect JWTs.
# Versions match tools/release/.venv as of 2026-08. Re-pin deliberately.
PyJWT~=2.12.1
cryptography~=46.0.5
requests~=2.32.5
PyYAML~=6.0.3
playwright~=1.58.0
